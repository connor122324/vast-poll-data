# vast-poll-data
Vast.ai GPU marketplace snapshots from the public `console.vast.ai/api/v0/bundles/` endpoint, polled every 30 minutes for five cards (B200, B300, H200 NVL, A100 SXM4, RTX 4090). `data.js` is regenerated after each poll and feeds a live dashboard. Market statistics only.
